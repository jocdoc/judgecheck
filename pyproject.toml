[project]
name = "judgecheck"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "numpy",
    "pandas",
    "scipy",
    "openpyxl",
    "pdfplumber",
    "psycopg2-binary",
]

[tool.vercel]
entrypoint = "api.analyze:handler"
